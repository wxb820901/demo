drop table if EXISTS PEOPLE;

CREATE TABLE PEOPLE (id BIGINT generated by default as identity, name VARCHAR(255), CREATED_DATE_TIME TIMESTAMP, IS_INTEGRATED BOOLEAN DEFAULT FALSE, PRIMARY KEY (id));

CREATE SEQUENCE HIBERNATE_SEQUENCE START WITH 1 INCREMENT BY 1;
